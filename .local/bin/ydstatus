#!/bin/python3
import subprocess
import time

try:
    while True:
        subprocess.run(["yandex-disk", "status"])
        time.sleep(3)
        subprocess.run(["clear"])
except KeyboardInterrupt:
    pass
